<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.ArGames" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
    </style>

    <style name="Theme.ArGames" parent="Base.Theme.ArGames" />

    <style name="Theme.ArGames.ProgressBarTheme">
        <item name="colorAccent">@color/white</item>
    </style>

    <style name="Theme.ArGames.SimpleCardView">
        <item name="cardElevation">0dp</item>
        <item name="strokeWidth">0dp</item>
    </style>

    <style name="Theme.ArGames.BigCardView">
        <item name="cardElevation">2dp</item>
        <item name="cardCornerRadius">16dp</item>
    </style>
</resources>